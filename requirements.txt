# FormaFix — Runtime dependencies
# Install with:  pip install -r requirements.txt

# UI Framework
flet>=0.21.0

# Computer vision & pose estimation
mediapipe>=0.10.0
opencv-python>=4.8.0
numpy>=1.24.0

# AI providers
anthropic>=0.20.0     # optional: only if AI_PROVIDER=anthropic
# google-generativeai is accessed via raw HTTP — no extra package needed

# Audio feedback
pyttsx3>=2.90

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# API backend
fastapi>=0.115.0
uvicorn>=0.30.0
